import { runAllTests } from "./LoggerColorTest";

/**
 * Simple script to test logger colors
 *
 * Run with:
 * npx ts-node src/examples/test-logger-colors.ts
 *
 * Or with specific debug namespaces:
 * DEBUG=lan-transfer:* npx ts-node src/examples/test-logger-colors.ts
 */

// Run all logger color tests
runAllTests();
